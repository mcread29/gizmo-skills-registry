import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export const gizmoExtension = {
  id: "agent-journal",
  name: "Agent Journal",
};

export default function agentJournal(_pi: ExtensionAPI) {}
